{
  "name": "notebook-cell-index",
  "displayName": "Notebook Cell Index",
  "description": "Number every cell of every notebook with click-to-jump navigation, an active-cell status bar indicator, custom label templates (Cell {n}, {section}.{n}, In[{exec}], {sectionTitle} · {n}), Jupyter-style execution numbering, section-aware counter resets, copy-cell-reference command, and per-kind toggles for code and markdown.",
  "version": "0.5.0",
  "publisher": "KolosKerekes",
  "license": "MIT",
  "author": {
    "name": "Kolos Kerekes"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/koloskerekes/Notebook-Cell-Index.git"
  },
  "homepage": "https://github.com/koloskerekes/Notebook-Cell-Index#readme",
  "bugs": {
    "url": "https://github.com/koloskerekes/Notebook-Cell-Index/issues"
  },
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#1f1f1f",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Notebooks",
    "Visualization",
    "Other"
  ],
  "keywords": [
    "notebook",
    "jupyter",
    "cell",
    "index",
    "number",
    "numbering",
    "status bar",
    "ipynb",
    "navigation",
    "jump",
    "picker",
    "outline",
    "execution",
    "template",
    "section",
    "polyglot"
  ],
  "activationEvents": [
    "onNotebook:*"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "notebookCellIndex.openCellPicker",
        "title": "Jump to Cell…",
        "category": "Notebook Cell Index"
      },
      {
        "command": "notebookCellIndex.copyCellReference",
        "title": "Copy Cell Reference",
        "category": "Notebook Cell Index"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "notebookCellIndex.openCellPicker",
          "when": "notebookEditorFocused"
        },
        {
          "command": "notebookCellIndex.copyCellReference",
          "when": "notebookEditorFocused"
        }
      ]
    },
    "configuration": {
      "title": "Notebook Cell Index",
      "properties": {
        "notebookCellIndex.format": {
          "order": 1,
          "type": "string",
          "enum": ["Cell N", "#N", "[N]"],
          "enumDescriptions": [
            "Label format: 'Cell 1', 'Cell 2', ...",
            "Compact: '#1', '#2', ...",
            "Bracketed: '[1]', '[2]', ..."
          ],
          "default": "Cell N",
          "description": "Preset label format. Ignored if `customFormat` is set."
        },
        "notebookCellIndex.customFormat": {
          "order": 2,
          "type": "string",
          "default": "",
          "description": "Custom label template (e.g. 'Cell {n}/{total}', '{section}.{n}', 'In[{exec}]', '{sectionTitle} · {n}'). Tokens: {n}, {n0}, {total}, {grandTotal}, {section}, {sectionTitle}, {kind}, {exec}. {sectionTitle} resolves to the heading text of the markdown cell that opened the current section (requires `resetCounterOnSection`). Cells without an execution count are hidden when {exec} is used. Empty falls back to `format`."
        },
        "notebookCellIndex.showTotal": {
          "order": 3,
          "type": "boolean",
          "default": false,
          "description": "Show the total cell count alongside the index (e.g. 'Cell 2/5'). Has no effect if the template already contains {total}."
        },
        "notebookCellIndex.alignment": {
          "order": 4,
          "type": "string",
          "enum": ["left", "right"],
          "default": "left",
          "description": "Alignment of the cell-number badge in the cell status bar."
        },
        "notebookCellIndex.hideWhenSingle": {
          "order": 5,
          "type": "boolean",
          "default": false,
          "description": "Hide the cell-number badge when the notebook has 0 or 1 displayed cells."
        },
        "notebookCellIndex.showCodeNumbers": {
          "order": 6,
          "type": "boolean",
          "default": true,
          "description": "Show numbering on code cells."
        },
        "notebookCellIndex.showMarkdownNumbers": {
          "order": 7,
          "type": "boolean",
          "default": false,
          "description": "Show numbering on markdown cells. When enabled, markdown cells receive a status-bar number and count toward the total."
        },
        "notebookCellIndex.resetCounterOnSection": {
          "order": 8,
          "type": "string",
          "enum": ["none", "h1", "h1-h2"],
          "enumDescriptions": [
            "Number cells continuously across the whole notebook.",
            "Restart the counter at every H1 (`# `) markdown cell.",
            "Restart the counter at every H1 (`# `) or H2 (`## `) markdown cell."
          ],
          "default": "none",
          "description": "Restart the counter when a markdown cell starts with the configured heading level. Combine with `customFormat` and the `{section}` token to display hierarchical labels like '1.3'."
        },
        "notebookCellIndex.clickToJump": {
          "order": 9,
          "type": "boolean",
          "default": true,
          "description": "Make each cell's status-bar badge clickable. Click opens a quick-pick of all cells with live preview as you arrow through it."
        },
        "notebookCellIndex.showGlobalStatusItem": {
          "order": 10,
          "type": "boolean",
          "default": true,
          "description": "Show the active cell's number in the window status bar (bottom right). Click it to open the cell picker."
        }
      }
    }
  },
  "scripts": {
    "compile": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --minify",
    "watch": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --watch",
    "typecheck": "tsc --noEmit",
    "vscode:prepublish": "npm run typecheck && npm run compile",
    "package": "npx @vscode/vsce package",
    "publish": "npx @vscode/vsce publish"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
